/*const fighterSort = () => {
    const newFighterOrder = partyStatus.sort((fighterA, fighterB) => {
        if (fighterA.player.order < fighterB.player.order) return -1;
        if (fighterA.player.order > fighterB.player.order) return 1;
        return 0;
    })
    setFighterOrder(newFighterOrder)

    const test = async () => {
        await foo(2)
        console.log("foo");
        await foo(1)


    }

    const foo = (order) => new Promise((resolve) => setTimeout(() => {
        setOrder(order)
    }, 2000));


    useEffect(() => { test(); }, [])

}*/